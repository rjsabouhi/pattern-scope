Got you.
Here is exactly what Replit needs to build so you can:
	1.	Generate a clean, standalone Python package
	2.	Export it as a ZIP or GitHub repo
	3.	Upload it to PyPI
	4.	With zero Replit fingerprints

This is the complete, minimal, correct package structure that Replit should output — no hidden .replit, no .config, no virtual environment metadata, no .pyc, no caches.

Everything below is all files, all contents, and only the files that should appear on GitHub + PyPI.

Just feed this entire block to Replit.

⸻

✅ THE EXACT DIRECTORY STRUCTURE (Replit-safe, PyPI-safe)

patternscope/
│
├── patternscope/
│   ├── __init__.py
│   ├── core.py
│   ├── metrics.py
│   ├── visualize.py
│
├── tests/
│   ├── test_core.py
│   ├── test_metrics.py
│
├── README.md
├── pyproject.toml
├── LICENSE

NOTHING ELSE.

No:
	•	.replit
	•	replit.nix
	•	.pythonlibs
	•	.pytest_cache
	•	__pycache__
	•	.DS_Store
	•	uv.lock
	•	virtualenv folders

This is exactly what PyPI wants.

⸻

📄 FILE CONTENTS — COPY INTO REPLIT DIRECTLY

patternscope/__init__.py

from .core import analyze

__all__ = ["analyze"]


⸻

patternscope/core.py

import pandas as pd
import numpy as np
from .metrics import monotonic_score, outlier_score, inflection_points
from .visualize import plot_patterns


def analyze(csv_path, plot=False):
    """
    Analyze numerical columns in a CSV file.
    Returns a dict of monotonicity, outlier count, and inflection points.
    """

    df = pd.read_csv(csv_path)
    summary = {}

    for col in df.columns:
        # Only analyze numeric columns
        if not np.issubdtype(df[col].dtype, np.number):
            continue

        series = df[col].dropna().values

        summary[col] = {
            "monotonicity": monotonic_score(series),
            "outliers": outlier_score(series),
            "inflections": inflection_points(series),
        }

        if plot:
            plot_patterns(series, title=col)

    return summary


⸻

patternscope/metrics.py

import numpy as np
from scipy.signal import find_peaks


def monotonic_score(x):
    """Returns score in [0,1] measuring monotonic consistency."""
    diffs = np.diff(x)
    sign = np.sign(np.sum(diffs))
    return float(np.mean(np.sign(diffs) == sign))


def outlier_score(x):
    """Counts |z| > 3 outliers."""
    z = (x - np.mean(x)) / (np.std(x) + 1e-8)
    return int(np.sum(np.abs(z) > 3))


def inflection_points(x):
    """Counts curvature shifts via 2nd derivative peaks."""
    second = np.diff(x, n=2)
    peaks, _ = find_peaks(np.abs(second), height=np.std(second))
    return int(len(peaks))


⸻

patternscope/visualize.py

import matplotlib.pyplot as plt


def plot_patterns(x, title="Series"):
    plt.figure(figsize=(6, 3))
    plt.plot(x, label="data", linewidth=1.3)
    plt.title(f"Pattern Visualization: {title}")
    plt.tight_layout()
    plt.show()


⸻

tests/test_core.py

from patternscope.core import analyze
import pandas as pd


def test_basic():
    df = pd.DataFrame({"x": [1,2,3,4,5]})
    df.to_csv("tmp.csv", index=False)
    result = analyze("tmp.csv")
    assert "x" in result


⸻

tests/test_metrics.py

from patternscope.metrics import monotonic_score, outlier_score, inflection_points


def test_monotonic():
    assert monotonic_score([1,2,3,4]) == 1.0

(This file can stay minimal.)

⸻

README.md

# patternscope

A tiny (<200 lines) CSV pattern-analysis tool.

It computes:
- Monotonicity score
- Outlier count
- Inflection-point estimates
- Optional matplotlib plots

## Install

pip install patternscope

## Usage

from patternscope.core import analyze
summary = analyze("data.csv", plot=True)
print(summary)


⸻

pyproject.toml

[project]
name = "patternscope"
version = "0.1.0"
description = "Tiny CSV pattern analysis tool."
readme = "README.md"
license = "MIT"
authors = [{ name = "Ryan Sabouhi" }]
requires-python = ">=3.8"

dependencies = [
    "pandas",
    "numpy",
    "scipy",
    "matplotlib",
]

[project.urls]
Homepage = "https://github.com/rjsabouhi/patternscope"


⸻

LICENSE (MIT)

MIT License

Copyright (c) 2026 Ryan Sabouhi

Permission is hereby granted...